# This file is auto-generated by openmediavault (https://www.openmediavault.org)
# WARNING: Do not edit this file, your changes will get lost.

# uptime-kuma
# 
version: '3.8'
services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    container_name: uptime-kuma
    volumes:
      - ./uptime-kuma:/app/data
    #ports:
    #  - 3002:3001  # <Host Port>:<Container Port>
    restart: always
      # https://github.com/linuxserver/docker-mods/tree/swag-auto-proxy
    #labels: 
    #  - swag=enable #- required for auto-detection
      #- swag_address=uptime-kuma #- optional - overrides upstream app address. Can be set to an IP or a DNS hostname. Defaults to container name.
      #- swag_port=3001 - optional #- overrides internal exposed port (if no preset conf and this label not set, auto-proxy will default to first detected exposed port)
      #- swag_proto=http - optional #- overrides internal proto (defaults to http)
      #- swag_url=uptime-kuma.virtual-lab.pro #- optional - overrides server_name (defaults to containername.*)
      #- swag_auth=authelia - optional #- enables auth methods (options are authelia, authentik, ldap and http for basic http auth)
      #- swag_auth_bypass=/api,/othersubfolder - optional - bypasses auth for selected subfolders. Comma separated, no spaces.
      #- swag_server_custom_directive=custom_directive; #- optional - injects the label value as is into the server block of the generated conf. Must be a valid nginx directive, ending with a semi colon.
    networks:
      swag: 
      
networks:
  swag:
    external: true
